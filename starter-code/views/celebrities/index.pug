extends ../layout

block content 
  a(href="/") HOME
  h2 These are all the celebrities saved in DB.
  each celebritie in celebrities
   ul
    a(href=`/celebrities/${celebritie._id}`)
      li #{celebritie.name}
      form(action=`celebrities/${celebritie._id}/delete` method="POST")
        button(type="submit") Delete